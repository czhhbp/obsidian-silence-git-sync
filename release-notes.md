# 更新日志

## 1.0.0

首个正式版本。

### 功能

- **仓库关联**：设置中填入仓库 HTTPS 地址即可，自动创建或更新 `origin` 远程；留空时沿用笔记库已有的 Git 配置。
- **静默后台同步**：停止编辑指定时间后自动同步，同时支持固定间隔的定时同步，全程不弹出提示。
- **冲突零丢失**：合并冲突时本地原文保留，远程版本另存为 `<名称>.sync-remote<扩展名>`，两者一并提交。
- **忽略路径托管**：将设置中的路径写入库根 `.gitignore` 的 `# silence-git-sync` 标记区间，并通过 `git ls-files -ci --exclude-standard` 让规则对已跟踪文件立即生效。留空时默认忽略所有点号开头的文件与文件夹。
- **访问令牌支持**：可选填写 Personal Access Token，通过临时 `GIT_CONFIG_GLOBAL` 注入 `http.extraHeader`，不落盘到 `.git/config`。
- **状态栏回显**：状态栏显示 `上次同步：HH:MM:SS 成功/失败`。
- **手动同步入口**：左侧功能区图标，以及 `Ctrl/Cmd + Shift + S` 快捷键。

### 说明

- 仅支持桌面端。移动端环境下插件可正常加载，但同步入口会被隐藏，并在状态栏提示不可用。
- 未检测到系统 Git 时会给出明确的安装指引，而不是静默失败。
- 后台同步默认静默，但「缺少系统 Git」「环境不支持」这类配置问题仍会提示。
